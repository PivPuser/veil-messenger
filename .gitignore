# Dart / Flutter
.dart_tool/
.packages
build/
# Packages regenerate this via `dart pub get`; not committed in this repo.
pubspec.lock
.flutter-plugins
.flutter-plugins-dependencies

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Secrets — never commit real keys or identities
*.key
*.pem
secrets/
